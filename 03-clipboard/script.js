const txt = document.querySelector("#txt");
const button = document.querySelector("#copy");

button.addEventListener("click", () => {
  // navigator.clipboard.writeText(txt.value);
  // navigator.clipboard.readText().then((clip) => alert(clip));^
  
});

